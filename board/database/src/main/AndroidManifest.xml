<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- 无侵入式数据库初始化 Provider -->
        <provider
            android:name=".DatabaseInitProvider"
            android:authorities="${applicationId}.database.init"
            android:exported="false"
            android:enabled="true" />
    </application>

</manifest>
