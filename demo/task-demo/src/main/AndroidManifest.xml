<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskDemo">
        
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".basic.BasicUsageActivity" />
        <activity android:name=".state.StateCallbackActivity" />
        <activity android:name=".progress.ProgressReportActivity" />
        <activity android:name=".priority.PriorityQueueActivity" />
        <activity android:name=".concurrent.ConcurrentDownloadActivity" />
        <activity android:name=".lifecycle.LifecycleActivity" />
        <activity android:name=".lifecycle.LifecycleTestActivity" />

    </application>

</manifest>
