# App Test Resources (动态功能模块)

演示 Android App Bundle 动态交付（On-Demand Delivery）的动态功能模块。

## 功能

- 按需交付模式（`dist:on-demand`）
- 被 `app-test` 通过 Play Core 库按需加载

## 模块类型

Android Dynamic Feature（`com.cn.app.test.resources`）
